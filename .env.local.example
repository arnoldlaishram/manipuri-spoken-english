# Copy this file to .env.local and put your key in it.
#   cp .env.local.example .env.local
#
# The key is read ONLY on the server (app/api/claude/route.ts).
# It is never sent to the browser.
ANTHROPIC_API_KEY=sk-ant-...
